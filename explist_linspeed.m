% explist(end+1).monk = 41;       % Monkey # 
% explist(end).sess = 44;         % Session #
% explist(end).run = 1;           % Run #
% explist(end).chan = 1:16;       % Channel #
% explist(end).row = 20;          % Row (grid)
% explist(end).column = 21;       % Column (grid)
% explist(end).depth = 1600;      % Depth below gray matter (microns)
% explist(end).spacing = 100;     % spacing between channels (microns)

explist = [];

explist(end+1).monk = 41;
explist(end).sess = 44;
explist(end).run = 1;
explist(end).chan = 1:16;
explist(end).row = 20;
explist(end).column = 21;
explist(end).depth = 1600; 
explist(end).spacing = 100; 

% %%%%%%%%%%%%%
% %%%%%%%%%%%%%
explist(end+1).monk = 41;  
explist(end).sess = 45;   
explist(end).run = 1;        
explist(end).chan = 1:16;  
explist(end).row = 19;       
explist(end).column = 17;    
explist(end).depth = 1600;
explist(end).spacing = 100;

% %%%%%%%%%%
% %%%%%%%%%%
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 46;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 18;    % Column (grid)
% explist(end).depth = 1600;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(1).monkey = 41;    % Monkey # 
% explist(end).session = 47;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 20;    % Column (grid)
% explist(end).depth = 1476;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% % explist(end+1).monkey = 41;    % Monkey # 
% % explist(end).session = 48;   % Session #
% % explist(end).run = 1;        % Run #
% % explist(end).row = 19;       % Row (grid)
% % explist(end).column = 20;    % Column (grid)
% % explist(end).depth = 2400;   % Depth below gray matter (in microns)
% % explist(end).channel = 0;    % Channel #? (all channels=0)
% % explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%%
% %%%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 49;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 22;       % Row (grid)
% explist(end).column = 20;    % Column (grid)
% explist(end).depth = 1790 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% % 
% % %%%%%%%%%%%
% % %%%%%%%%%%%
% % 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 50;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 22;       % Row (grid)
% explist(end).column = 20;    % Column (grid)
% explist(end).depth = 3239 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 51;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 23;       % Row (grid)
% explist(end).column = 17;    % Column (grid)
% explist(end).depth =  1450;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 52;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 19;    % Column (grid)
% explist(end).depth = 1832 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 53;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 22;    % Column (grid)
% explist(end).depth = 1500;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 54;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = 22;    % Column (grid)
% explist(end).depth = 1600;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% 
% % explist(38).monkey = 41;    % Monkey # 
% % explist(38).session = 55;    % Session #                                     
% % explist(38).run = 1;        % Run #
% % explist(38).row = 23;       % Row (grid)
% % explist(38).column = 19;    % Column (grid)
% % explist(38).depth = 1200 ;  % Depth below gray matter (in microns)
% % explist(38).channel = 0;    % Channel #? (all channels=0)
% % explist(38).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 56;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = -18;    % Column (grid)
% explist(end).depth = 1853;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 57;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = -18;    % Column (grid)
% explist(end).depth = 3550;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 58;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = -17;    % Column (grid)
% explist(end).depth = 1597;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 59;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = -17;    % Column (grid)
% explist(end).depth = 2451;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%% 
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 60;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = -17;    % Column (grid)
% explist(end).depth = 2100;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 61;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = -17;    % Column (grid)
% explist(end).depth = 3400;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 62;   % Session #
% explist(end).run =1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = -20;    % Column (grid)
% explist(end).depth = 1568;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 63;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = -20;    % Column (grid)
% explist(end).depth = 2450;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 41;    % Monkey # 
% explist(end).session = 64;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = -21;    % Column (grid)
% explist(end).depth = 1690;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)

%% eric's data
% 
% explist(end+1).monkey = 45;    % Monkey # 
% explist(end).session = 24;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 20;    % Column (grid)
% explist(end).depth = 2105;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% % 
% % %%%%%%%%%%%%%
% % %%%%%%%%%%%%%
% % 
% explist(end+1).monkey = 45;    % Monkey # 
% explist(end).session = 25;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 15;    % Column (grid)
% explist(end).depth = 2313;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% 
% explist(end+1).monkey = 45;    % Monkey # 
% explist(end).session = 26;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 13;    % Column (grid)
% explist(end).depth = 2785;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% % 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 45;    % Monkey # 
% explist(end).session = 27;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 13;    % Column (grid)
% explist(end).depth = 2298;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 45;    % Monkey # 
% explist(end).session = 28;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = 14;    % Column (grid)
% explist(end).depth = 1700;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% %%%%%%%%%%%%%
% %%%%%%%%%%%%%
% % 
% % %%% Quigley  %%%
% % 
% % %%%%%%%%%%%%%
% % %%%%%%%%%%%%%
% % 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 1;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 18;       % Row (grid)
% explist(end).column = 19;    % Column (grid)
% explist(end).depth = 1708 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% % 
% % %%%%%%%%%%%
% % %%%%%%%%%%%
% % 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 2;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 18;    % Column (grid)
% explist(end).depth = 3183 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 3;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 21;    % Column (grid)
% explist(end).depth =  4168;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 4;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 17;       % Row (grid)
% explist(end).column = 17;    % Column (grid)
% explist(end).depth = 3390 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 5;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 16;       % Row (grid)
% explist(end).column = 19;    % Column (grid)
% explist(end).depth = 3045;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 6;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 18;       % Row (grid)
% explist(end).column = 15;    % Column (grid)
% explist(end).depth = 3784;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 7;    % Session #                                     
% explist(end).run = 1;        % Run #
% explist(end).row = 19;       % Row (grid)
% explist(end).column = 18;    % Column (grid)
% explist(end).depth = 3040 ;  % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 8;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 17;       % Row (grid)
% explist(end).column = 21;    % Column (grid)
% explist(end).depth = 3330;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 9;    % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 18;       % Row (grid)
% explist(end).column = 18;    % Column (grid)
% explist(end).depth = 3313;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 10;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 15;    % Column (grid)
% explist(end).depth = 3900;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 11;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 16;       % Row (grid)
% explist(end).column = 16;    % Column (grid)
% explist(end).depth = 2591;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%% 
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 13;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = 13;    % Column (grid)
% explist(end).depth = 2468;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 14;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = 17;    % Column (grid)
% explist(end).depth = 2201;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 16;   % Session #
% explist(end).run =1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 13;    % Column (grid)
% explist(end).depth = 2584;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 17;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 18;       % Row (grid)
% explist(end).column = 20;    % Column (grid)
% explist(end).depth = 3300;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 18;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 16;       % Row (grid)
% explist(end).column = 17;    % Column (grid)
% explist(end).depth = 2247;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%%
% %%%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 19;   % Session #
% explist(end).run = 1;        % Run #
% explist(end).row = 20;       % Row (grid)
% explist(end).column = 19;    % Column (grid)
% explist(end).depth = 3687;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%
% %%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 20;   % Session #
% explist(end).run = 1;       % Run #
% explist(end).row = 17;       % Row (grid)
% explist(end).column = 19;    % Column (grid)
% explist(end).depth = 2594;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%%
% %%%%%%%%%%%
% 
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session = 21;   % Session #
% explist(end).run = 1;       % Run #
% explist(end).row = 21;       % Row (grid)
% explist(end).column = 14;    % Column (grid)
% explist(end).depth = 2491;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% % explist(51).units = 0;      % All units? (sorted units=0; all units=1)
% 
% %%%%%%%%%%
% %%%%%%%%%%
% explist(end+1).monkey = 44;    % Monkey # 
% explist(end).session =22;   % Session #
% explist(end).run = 1;       % Run #
% explist(end).row = 18;       % Row (grid)
% explist(end).column = 14;    % Column (grid)
% explist(end).depth = 3434;   % Depth below gray matter (in microns)
% explist(end).channel = 0;    % Channel #? (all channels=0)
% explist(end).units = 0;      % All units? (sorted units=0; all units=1)